// prismaProduct.js
// Prisma schema representation of the Mongoose Product model
// This is for documentation/reference only. Actual schema is in prisma/schema.prisma

/*
model Product {
  id          Int        @id @default(autoincrement())
  name        String
  image_uri   String
  price       Float
  ar_uri      String?
  description String?
  category    Category   @relation(fields: [categoryId], references: [id])
  categoryId  Int
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}
*/
